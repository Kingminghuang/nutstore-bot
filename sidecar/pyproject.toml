[project]
name = "python-runtime"
version = "0.1.0"
description = "Python runtime for the native code agent"
requires-python = ">=3.11"
dependencies = [
  "litellm>=1.51.0",
  "requests",
  "smolagents",
]

[dependency-groups]
dev = [
  "pytest>=8.3.0",
]

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
